# Shared runtime and evaluation dependencies for the vggt28 environment.
#
# Install torch==2.8.0 and torchvision==0.23.0 from the CUDA 12.8 index
# *before* installing this file.  They are deliberately excluded here so pip
# cannot replace the CUDA 12.8 / Blackwell-compatible wheel.
#
# Covered paths:
#   VGGT / VGGT-Omega / Pi3:
#   baseline, FastVGGT, DA-VGGT, Sparse-VGGT, U-M, and AVGGT.

# Core model/runtime packages
numpy==1.26.4
Pillow>=10.3.0
einops>=0.8.1
huggingface_hub>=0.34.4
safetensors>=0.6.2
omegaconf>=2.3.0
hydra-core>=1.3.2,<1.4
rootutils>=1.0.7
PyYAML>=6.0
plyfile>=1.1.2

# Image I/O, official evaluation, and common geometry metrics
opencv-python>=4.10.0
scipy>=1.13.0
open3d>=0.18.0
imageio>=2.34.0
tqdm>=4.66.0
evo>=1.29.0
matplotlib>=3.8.0
scikit-image>=0.22.0
trimesh>=4.0.0
pyceres>=2.4.0
pycolmap>=3.10.0

# Pi3 / U-M runtime dependencies.  These are deliberately explicit: Pi3's
# local package metadata only declares safetensors, while its runtime imports
# this stack transitively on experiment machines.
pypose>=0.7.0
jaxtyping>=0.3.0
beartype>=0.20.0
typeguard>=4.0.0
wadler-lindig>=0.1.3
typing-inspection>=0.4.0
rich>=13.7.0
pytest>=7.0.0

# FastVGGT-compatible utility dependencies used by the shared VGGT code.
fvcore>=0.1.5.post20221221
iopath>=0.1.10
yacs>=0.1.8
termcolor>=2.4.0
tabulate>=0.9.0
kornia>=0.8.0
kornia_rs>=0.1.9
addict>=2.4.0

# Sparse-VGGT / SpargeAttn build and runtime support
ninja>=1.13.0
